language: php

services:
  - docker
  
before_install:
  # Run TestSuite
  - cd ./test_suite
  - docker-compose up -d
  - cd ..
  - sleep 60  # Wait for TestSuite is ready
  - composer install
  
php:   
#  - "5.6"
  - "7.3"
#  - "nightly"  # nightly build

script: phpunit ./tests
